import { styled } from "@/styles";

export const FooterContainer = styled('footer', {
  position: 'absolute',
  bottom: 0,
  backgroundColor: '#EEE',
  width: '100%',
  textAlign: 'center',
  paddingBlock: 8,
})